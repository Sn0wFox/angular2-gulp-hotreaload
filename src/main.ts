import {bootstrap} from '@angular/platform-browser-dynamic';   // Angular's browser boostrap function (essential)
import {AppComponent} from './app/app.component';             // The root component for our app

// Aaaaaaand... Start it !
bootstrap(AppComponent);
